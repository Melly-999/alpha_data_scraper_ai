# Optional: backend API base URL. Leave empty to use the Vite /melly-api proxy.
VITE_MELLY_API_BASE_URL=/melly-api

# Optional: read-only API key for protected dashboard endpoints.
# Use a local value in .env.local; do not commit real keys.
VITE_MELLY_API_KEY=change-me-local-readonly-key
