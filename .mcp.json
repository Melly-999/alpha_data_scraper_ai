{
  "$schema": "https://modelcontextprotocol.io/schema/config.json",
  "_comment": "CloudMCP.run router wiring — a single MCP endpoint fans out to all backend servers (files, web, memory). Set CLOUDMCP_ROUTER_URL and CLOUDMCP_ROUTER_TOKEN in your local .env (never committed).",
  "mcpServers": {
    "cloudmcp-router": {
      "url": "${CLOUDMCP_ROUTER_URL}",
      "headers": {
        "Authorization": "Bearer ${CLOUDMCP_ROUTER_TOKEN}"
      }
    }
  }
}
