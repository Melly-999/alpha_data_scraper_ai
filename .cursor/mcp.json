{
  "$schema": "https://modelcontextprotocol.io/schema/config.json",
  "_comment": "Cursor-local CloudMCP.run router wiring. Same single-URL, URL-auth model as repo-root .mcp.json; CLOUDMCP_ROUTER_URL comes from the developer's local .env (never committed).",
  "mcpServers": {
    "cloudmcp-manager": {
      "type": "http",
      "url": "${CLOUDMCP_ROUTER_URL}"
    }
  }
}
